# Protocol and Transport Standards

This directory defines wire formats, propagation mechanisms, and exporter contracts.

## Definitions

| Component | Standard |
|-----------|----------|
| Context Propagation | W3C Trace Context + custom headers |
| Log Exporter Format | JSON (stdout/file), JSON over HTTP, OTLP |
